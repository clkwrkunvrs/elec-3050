FUNC void dispalyvalues(void) {
	int idx;
	
	exec("log > sampleArray2.csv");
	for (idx = 0; idx < 1000; idx++) {
			printf ("samples[%02u] = %02u\n", idx, sampleArray[idx]);
	}
	exec("log off");
}